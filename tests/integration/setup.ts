// setup file for integration tests
// You can add global setup/teardown logic here if needed

beforeAll(async () => {
  // Example: Check Docker is running before tests
  // Optionally, you can skip tests if Docker is not available
});

afterAll(async () => {
  // Cleanup logic if needed
});
